# Hs-CNT_2026-05 / reference

Reference benchmarks — gold/silver commodities, nuclear SEMF (2 experiments).

Each experiment is a self-contained record:

* `<id>_input.csv` — input data (where bundled; domain inputs are
  produced by the adapters in `HCI-CNT/adapters/` against `DATA/`)
* `<id>_cnt.json` — canonical engine output (engine 2.0.4, schema 2.1.0)
* `JOURNAL.md` — auto-generated narrative summary

For the full corpus inventory and verification recipe, see the parent
[`README.md`](../README.md).

---

*The instrument reads. The expert decides. The hashes carry the receipts.*
