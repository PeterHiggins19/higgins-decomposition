# Hs — Higgins Decomposition
# Minimal runtime dependencies for the canonical CNT and CNQ engines.
#
# Install with:
#   pip install -r requirements.txt
#
# Or install the package itself (recommended; uses pyproject.toml):
#   pip install -e .

numpy>=1.20

# Optional dev dependencies (uncomment to enable the test suite):
# pytest>=7.0

# Optional plate / atlas generators (uncomment if you need stage1/2/3/4 plates):
# matplotlib>=3.4
# pandas>=1.3
