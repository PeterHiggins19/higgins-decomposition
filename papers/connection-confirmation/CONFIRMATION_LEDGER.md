# Confirmation ledger — the suspicious items, tested

*Author: Peter Higgins (human authorship for all claims); AI-assisted per HUF-STD-001. 2026-06-25. The
honest-broker discipline turned on **our own** unproven claims: take the AI-candidate connections (C1–C9 in
`../UNWRITTEN_CONNECTIONS_SEEDS.md`), test the ones that are runnable now to confirmation, and **fill the
rest with the honest remainder** — what could not be confirmed by a quick run, and why. Receipt
`09fff0925dd49fab` (`confirm_suspicious.py`). Nothing is promoted past what the test supports. Peter is the
sole gate; nothing posted.*

---

## Verdicts

| # | claim | tested? | verdict | what was measured |
|---|---|---|---|---|
| **C2** | ground state ↔ coherence law ↔ fiber/probe common-mode are **one theorem** | yes | **CONFIRMED** | `clr(g·x)=clr(x)` to `8.9×10⁻¹⁶`; the coherence law `−10·log₁₀(1−ρ)` reproduces (ρ=0.9→10 dB, 0.99→20 dB, …); the 313/310 dB anchors are its ρ→1 floor. **One result, three costumes.** |
| **C7** | contact-point `R = contacts × drift` ↔ network centrality | yes | **CONFIRMED (identity)** | `R` equals **weighted-degree centrality** when drift is the edge weight — Spearman **0.998** vs a noisy estimate. The "highest-contact = highest-leverage" doctrine *is* a known centrality. |
| **C5** | weight concentrates while denomination diversifies (coupled compositions) | yes (2 layers) | **PARTIAL-CONFIRMED** | GDP eff-dim **7.3→6.2** (concentrates) while reserve-currency eff-dim **2.47→3.50** (diversifies): the anti-correlation **holds on both measured layers.** |
| **C4** | coherence law ↔ an information-theory bound | yes | **PARTIAL** | the law shares the **exact log-singularity** of the Gaussian mutual-information bound at ρ→1 (a real kinship), but differs by the `(1+ρ)` factor at finite ρ — **same divergence structure, not the same expression.** |
| **C1** | blindness suite is a complete/dual face-basis | no | **NOT YET — math** | needs a proof that a finite complete face-basis exists (or is provably open). Stays **T3**. |
| **C3** | dimension-is-the-message ↔ face-count (capacity↔faces) | no | **NOT YET — math+data** | needs derived `capacity(D)` and `face-count(D)` + a real-data track. Stays **T3**. |
| **C6** | EITT invariance is an RG fixed point | no | **NOT YET — math** | needs the invariance derived from a coarse-graining (RG) operator. Stays **T3**. |
| **C8** | data-is-the-carrier ↔ MDL / Kolmogorov | no | **NOT YET — math** | needs a formal compositional-coding vs MDL bound. Stays **T3**. |
| **C9** | D=4 rung ↔ Hurwitz division-algebra ceiling | no | **NOT YET — speculative** | needs a classification of which real datasets admit an exact rung. Fence hard. Stays **T3**. |

## What got promoted (and to what)

- **C2 → confirmed, and worth a short note in the papers:** the RWA 313 dB ground-state result, the coherence
  law, and the fiber/probe common-mode rejection are **corollaries of a single exact-cancellation theorem**
  (`clr(g·x)=clr(x)`). This is the unification the corpus kept gesturing at, now demonstrated — a clean,
  publishable consolidation (not a new claim, a *single* statement of three known ones).
- **C7 → confirmed identity:** the contact-point doctrine has a name in graph theory (weighted-degree
  centrality). That *strengthens* it — it is standard, not ad hoc.
- **C5 → partial-confirmed:** a real, measured two-layer anti-correlation. Not yet a law.
- **C4 → partial:** a real information-theoretic kinship at the divergence, not an identity. Stated precisely.

## The remainder (filled honestly)

Five of the nine (C1, C3, C6, C8, C9) are **not confirmable by a quick run** — they are formal-math or
classification questions, not measurements. They **stay T3**, fenced, with the exact work each needs named
above. That is the honest gap-fill: the suspicious items that could be tested were tested; the ones that
could not are marked as such, with the reason, so no fence is mistaken for a result and no result is left
unclaimed.

*Cross-refs: `confirm_suspicious.py`, `CONFIRMATION_RESULTS.json`, `../UNWRITTEN_CONNECTIONS_SEEDS.md`
(statuses updated), `../../industrial-instruments/electronics-assembly-smt/COHERENCE_AND_LASERS.md` (C2),
`../../industrial-instruments/world-monetary-composition/` (C5). Peter is the sole gate; nothing posted.*

*Proof & Honesty Standard — our own claims tested · confirmed / partial / not-yet stated plainly · the remainder named, not hidden · experts decide.*
