# data/pipeline_output

Working folder for pipeline runs — held files from various Hs experiments. Each Hs-NN_* experiment has its own `pipeline_output/` inside its folder; this is the cross-experiment shared one.

For the canonical CNT-side adapter that processes this data, see
[`../../HCI-CNT/adapters/`](../../HCI-CNT/adapters/).

---

*The instrument reads. The expert decides. The hashes carry the receipts.*
