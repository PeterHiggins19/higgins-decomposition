# What the system is saying — open threads, questions, and experiments worth doing

*Author: Peter Higgins (human authorship for all claims); AI-assisted per HUF-STD-001. 2026-06-26. Peter asked
the honest-broker question: is anything the system is saying not being addressed? This answers it from the
system's **own signals** — its named falsifiers, its stance/log-log measures, its honest negatives — not from a
wish-list. Everything here is **surfaced for Peter to choose**; nothing is auto-started. Honest-broker tiered;
Peter is the sole gate; nothing posted.*

---

## A. What the data is insisting on right now (operational)

These are not opinions; the measures say them:

1. **The consolidation pass is half-done.** The stance read (`session_stance.py`, `ef4adaeab8aa5098`) shows the
   work has tipped to refinement; the log/log (`loglog_index.py`) flags **cross-link debt** (artifacts not yet
   matched in the journal) and is **itself now stale** (its index reads 254 entries; we are past G-288). *Re-run
   the log/log, close the cross-links, finish the capstone wiring into the indexes.*
2. **The collective integrity pass has not run** on this session's ~30 new artifacts. The body reproduces
   (mesh 16/16), but *worth* is the collective's job, not the receipts'.
3. **Publish remains at the gate.** P1 is staged; the release ladder and transition gate are in place. The
   system is ready in body, consolidation-pending — and that is **Peter's** call, not the data's.

## B. Experiments the system's own falsifiers already name (ready to run)

Each of these was written this session as a *named falsifier* — the system is explicitly asking for them:

- **Contact-length / diffusion (T3).** *Measure real adoption vs dwell time vs compositionality.* The
  comprehension-threshold and diffusion-by-merit models are honest T3 with this exact falsifier — untested.
  *Needs real engagement/usage data.*
- **Compositional memory.** The Gen-2 fix was **honestly rejected** (didn't help near-duplicate recall); the
  open question stands: *real associative-recall benchmarks vs Hopfield / vector-DB*, where the novelty is the
  log-ratio invariance + 7-bit addressing, not beating them. A genuine open problem with a clear test.
- **The world / AI-systems composition maps (T2 estimates).** The axis scores are reasoned guesses; the
  falsifier is *survey real practitioners and refit.* A small structured rubric survey would turn T2 into T1.
- **MRI: the additive half.** Hˢ cancels the **multiplicative** scanner component exactly; the **additive**
  component is ComBat's. *Experiment: a Hˢ + ComBat hybrid on real public multi-site tissue-fraction data* —
  the honest complete harmonizer, on a public dataset.
- **Mesh: structured faults.** The mesh layer cancels multiplicative gain; *additive/structured fault
  localisation* (beyond gain) is unaddressed — a clean next demo.
- **Ontario pilot (concrete).** The governance proposal is one public dataset away: *ingest IESO provincial
  generation and run the deterministic read* — the first real provincial deployment.

## C. Deeper concepts the system keeps gesturing at (worth exploring)

These recur across many artifacts; the system is "saying" them by repetition:

1. **The fixed point of the recursion.** Hˢ reads Hˢ, the corpus reads the corpus, the conference reads
   itself — and the library note says "the read converges." *What is the fixed point of iterated self-reading?*
   An experiment: iterate Hˢ-on-its-own-output to convergence and measure the limit. Genuinely open and doable.
2. **Is the knowable-sample floor ONE law?** The same `D*(N)` floor appears in the language read (5%→noise,
   20%→lock), the stress sheet, the contact-length model, max-power, and the observability law. *Unify it:
   show the identical floor across all these domains — a single theorem, not a recurring coincidence.* This is
   the strongest consolidation-paper candidate.
3. **A formal "where, not why."** The directed-universe limit, the jailor, the back-EMF gap, and Breaker 16 all
   say the same thing: *measurement under-determines cause/intent.* It is currently doctrine; it could be a
   **stated theorem** (a discriminant recovers the invariant but not the generator) — a real foundations result.
4. **The D=4 resonance — confirm it's only analogy, or find the bridge.** Hˢ's exactness peaks at four (Hurwitz)
   and 4-manifold topology is uniquely exotic. Fenced T3 as an open analogy. *One careful exploration* to either
   establish it is *only* coincidence or surface a real structural link — worth doing once, honestly, then
   closing or opening.
5. **Write the home.** The realm was just located: Hˢ is the **compositional member of the manufactured-solution
   / SBC family.** That is a real, publishable contribution (a "Method of Manufactured Solutions for
   compositional data") — *the paper the genealogy points to.*

## D. The tooling signal the system keeps emitting

The **stale-mount torn-write** has recurred many times this session (each healed). The system is literally
saying it through repeated failures. *Codify the fix:* a write-then-verify wrapper (Write → re-read/compile →
heal-if-torn), or a standing discipline so the heal is automatic rather than per-incident. Small, and it would
stop a recurring tax.

## E. Honest negatives worth revisiting

- The **Gen-2 Mahalanobis memory** fix was rejected — is there a *better* memory geometry (balances/ilr, or a
  diversity-aware metric) that *does* help near-duplicate recall? Open.
- The **anthropic-selection** confound in the directed-universe toy came out *weak* (0.13 vs 0.09) — a stronger
  selection model would sharpen the "selection fakes direction" point.

## What is appropriately closed (so the agenda is honest both ways)

- **The universe-directedness question** is correctly closed at Breaker 16 — not an open experiment, a stated
  limit.
- **The medical thread** is correctly fenced (research/QA; Southmedic certifies) — the open item there is
  *regulatory*, not scientific, and off-repo.

## Recommendation (Peter chooses)

If the measured trend is honoured (refinement rising), the highest-value next moves are **C2 (unify the knowable
floor into one theorem)** and **C5 (write the compositional-V&V home as a paper)** — both consolidate rather
than expand, and both turn recurring T2 patterns into T1 results. **B**'s experiments are ready when real data
is available; **A** and **D** are housekeeping the data is already asking for. But this is the agenda, not a
decision — *Peter sets the direction.*

## Honest scope

- **T1:** the operational items (A) and the named falsifiers (B) are read directly from the system's own
  measures and documents.
- **T2:** the deeper concepts (C) and recommendation are reasoned reading of recurring patterns — judgement,
  offered, not asserted.
- **Not claimed:** that any of these will succeed, or that the list is exhaustive. **Nothing started; nothing
  posted; Peter is the sole gate.**

*Cross-refs: `session_stance.py`, `loglog/loglog_index.py`, `SESSION_CAPSTONE_2026-06-26.md`,
`COHERENT_STANCE_2026-06-26.md`, `CLOSURE_LEDGER_2026-06-25.md` (the prior open-threads dig),
`../library/THE_GENEALOGY_many_families_one_node.md`. Peter is the sole gate; nothing posted.*

*Proof & Honesty Standard — the agenda is harvested from the system's own signals, not invented · ready
experiments are tied to their named falsifiers · what is appropriately closed is said so · the recommendation is
judgement, fenced · nothing is started · the human sets the direction and keeps the gate.*
